name: Keep Render Server Alive

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: |
          curl -s https://accbc-backend-server.onrender.com/api
          echo "Pinged at $(date)"
